# A2A SDK with HTTP server support (Starlette/ASGI).
# Pin version: AgentCard / AgentInterface schema (transport vs legacy protocolBinding) must match code.
a2a-sdk[http-server]==0.3.25
uvicorn>=0.30.0
python-dotenv>=1.0.0
httpx>=0.27.0
# MCP stdio client + bundled KB server (same tool protocol as Lab3).
mcp[cli]>=1.0.0
