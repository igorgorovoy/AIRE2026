apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Secret не включено: застосуйте вручну secrets-example.yaml (шаблон) або secrets.yaml,
  # інакше kubectl apply -k перезапише кластерний Secret прикладом з репозиторію.
  - assistant-agent.yaml
  - orchestrator-agent.yaml
